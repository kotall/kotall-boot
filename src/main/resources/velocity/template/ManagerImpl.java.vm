package ${package}.core.manager.${module}.impl;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import ${package}.common.dao.${module}.${className}Mapper;
import ${package}.common.entity.${module}.${className}Entity;
import ${package}.core.manager.${module}.${className}Manager;
import com.kotall.rms.core.manager.BaseManagerImpl;

/**
 * ${comments}
 *
 * @author ${author}
 * @date ${datetime}
 * @since 1.0.0
 */
@Component("${objName}Manager")
public class ${className}ManagerImpl extends BaseManagerImpl<${className}Mapper, ${className}Entity> implements ${className}Manager {

	@Autowired
	private ${className}Mapper ${objName}Mapper;
	
}
